# Replace mode, backup enabled, two profiles
# Expected: First profile replaces original, second creates _x265-fast suffix

mode: encode
profiles:
  - x265-balanced
  - x265-fast
source: replace/
output_mode: replace
backup: true
backup_dir: .originals
recursive: false
file_patterns:
  - "*.mkv"
  - "*.mp4"
