# Replace mode, backup enabled, single profile
# Expected: Original file replaced, backup created in .originals/

mode: encode
profile: x265-balanced
source: replace/
output_mode: replace
backup: true
backup_dir: .originals
recursive: false
file_patterns:
  - "*.mkv"
  - "*.mp4"
