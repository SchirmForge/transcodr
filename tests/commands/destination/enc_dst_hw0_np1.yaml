# Destination mode, NO hardware acceleration (software only), single profile
# Expected: Encoding uses CPU only

mode: encode
profile: x265-balanced
source: $root_media/original/
output_mode: destination
destination: $root_media/encoded/
hardware_accel: none
recursive: false
file_patterns:
  - "*.mkv"
  - "*.mp4"
